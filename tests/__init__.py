"""Test package for promptmigrate.

Run tests with pytest:

    pytest

Or with coverage:

    coverage run -m pytest
    coverage report
"""

# Tests are organized in modules:
# - test_manager.py: Tests for the PromptManager and attribute access
# - test_cli.py: Tests for the CLI interface (when added)
